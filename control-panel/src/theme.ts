import { createMuiTheme } from "@material-ui/core";

export default createMuiTheme({
  palette: {
    primary: {
      main: "#000000",
    },
    secondary: {
      main: "#c9cad4",
    },
    error: {
      main: "#A40000",
    },
    background: {
      default: "#fff",
    },
  },
});
